const std = @import("std");

pub fn checkStringInChoices(str: [] const u8, args: anytype) bool {
    _ = str;
    _ = args;

    // TODO   
    // var i: usize = 0;
    // const data: [][] const u8 = @as([][] const u8, choices);

    // while (i < data.len) {
    //     if(std.mem.eql(u8, str, data[i])){
    //         return true;
    //     }
    //     i += 1;
    // }

    return false;
}